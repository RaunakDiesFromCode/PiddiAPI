# PiddiAPI Local Development Configuration
# Set PIDDI_DEV=1 to enable the development bootstrap endpoint (/api/bootstrap)
# and allow loopback origins for the Vite development server (localhost:5173).
PIDDI_DEV=1
